# CLAUDE.md

## ⚠️ Read CONTEXT.md FIRST
Before doing any work in this repo, read **[CONTEXT.md](./CONTEXT.md)** — it holds the canonical facts about Kim/Jez's businesses (entities, email/domain setup, tool stack, products, and hard rules). When an assumption conflicts with CONTEXT.md, CONTEXT.md wins.

Also skim the most recent `session-handover-*.html` for in-flight context before starting.

## 🔒 GOLDEN RULE — THE MACHINES (never break this)
**Never recreate, redraw, restyle, "improve", reinterpret, or AI-generate the HIFU machines or Kim's clinic photos.** Always embed Kim's **actual photo files** (`machine-13d.png`, `7/8/9.jpg`, etc.) exactly as-is — **zero drift on the machine look**. If a needed photo is missing, use a clearly-marked placeholder and **ASK Kim** — never invent or substitute a machine image. Kim's images + style are the brand; AI must not "tidy" them.

## Quick rules
- Marketing = "HIFU Machine Sales" only (never "Neo-Klien").
- Email is **Google Workspace** (not Titan). Booking is **BookATreatment** (not Calendly).
- Use only the real assets in `funnel/brand-assets-manifest.md` (see Golden Rule above).
